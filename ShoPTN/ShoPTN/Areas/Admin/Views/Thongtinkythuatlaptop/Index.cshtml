@model IEnumerable<ShoPTN.Models.Thongtinkythuatlaptop>

@{
    ViewData["Title"] = "Index";
}

<div class="card border border-primary">
    <h5 class="card-header bg-primary text-white">@ViewData["Title"]</h5>
    <div class="card-body">
        <p>
            <a asp-action="Create" class="btn btn-primary">Tạo mới</a>
        </p>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Cpu)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Ram)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Gpu)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.IdProductNavigation)
                    </th>
                    <th width="150"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Cpu)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Ram)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Gpu)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.IdProductNavigation.TenSanPham)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.IdThongTin" class="btn btn-info">Sửa</a> |
                            <a asp-action="Delete" asp-route-id="@item.IdThongTin" class="btn btn-danger">Xóa</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
