@model ShoPTN.Models.InfomationTechology

@{
    ViewData["Title"] = "Delete";
}

<div class="card border border-danger">
    <h5 class="card-header bg-danger text-white">@ViewData["Title"]</h5>
    <div class="card-body">
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            Bạn có muốn xóa nhân viên <strong>@Model.Title</strong> không?
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>

        <div class="form-actions no-color">
            <form asp-action="Delete">
                <input type="hidden" asp-for="IdBaiViet" />
                <input type="submit" value="Xóa" class="btn btn-danger" /> |
                hoặc <a asp-action="Index">Trở về danh sách</a>
            </form>
        </div>
    </div>
</div>